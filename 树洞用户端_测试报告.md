# 树洞用户端 测试报告

| 项 | 内容 |
|---|---|
| 报告日期 | 2026-08-18 |
| 被测对象 | 树洞用户端（用户原声明地址 `http://localhost:80`） |
| 测试方式 | 浏览器自动化（CDP）+ HTTP 探测 + 进程/文件系统核查 |
| 测试结论 | **未通过（环境不可达）** — 服务在测试环境内并未运行，无法进行功能测试 |

---

## 1. 测试目标

按用户要求，对"树洞用户端 `http://localhost:80`"进行浏览器全功能测试，覆盖：

- 页面加载与首屏渲染
- 用户注册 / 登录
- 树洞发布 / 列表 / 详情
- 评论 / 点赞 / 收藏
- 个人中心 / 搜索 / 其他用户侧功能

并在测试完成后输出本报告与后端 API 文档。

## 2. 测试环境核查（前置检查）

在浏览器访问前，先对沙箱环境做了一次完整核查，结果如下：

### 2.1 端口监听情况

| 端口 | 用户声明 | 实际探测 | 实际监听进程 |
|---|---|---|---|
| 80 (树洞用户端) | ✅ 监听中 | HTTP 401 `{"error":"Unauthorized"}` | `agent-tool-host`（沙箱自身进程，非树洞服务） |
| 8081 (后端 API) | ✅ 监听中 | Connection refused（无监听） | 无 |
| 8082 (管理后台) | ✅ 监听中 | Connection refused（无监听） | 无 |

探测命令：
```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:80/    # -> 401
curl -s -o /dev/null -w "%{http_code}" http://localhost:8081/  # -> 000 (refused)
curl -s -o /dev/null -w "%{http_code}" http://localhost:8082/  # -> 000 (refused)
```

### 2.2 端口 80 多路径探测

对 `localhost:80` 的不同路径全部返回相同的 401 JSON，说明该端口被一个统一网关占用，而非树洞用户端：

| Path | HTTP Code | Body |
|---|---|---|
| `/` | 401 | `{"error":"Unauthorized"}` |
| `/index.html` | 401 | `{"error":"Unauthorized"}` |
| `/login` | 401 | `{"error":"Unauthorized"}` |
| `/api/` | 401 | `{"error":"Unauthorized"}` |
| `/api/v1` | 401 | `{"error":"Unauthorized"}` |
| `/health` | 401 | `{"error":"Unauthorized"}` |

### 2.3 进程核查

沙箱内运行的全部业务进程（已排除系统守护进程）：

| PID | 进程 | 说明 |
|---|---|---|
| 822 | `/app/bin/agent-tool-host` | 沙箱工具宿主（占用端口 80 等） |
| 1541 / 1575 | `mcp-server-fanqie` | 番茄小说 MCP，与树洞无关 |

**未发现**任何 Spring Boot / Express / Next.js / Vite / Nginx / Django / Flask 等典型 Web 服务进程。即：后端、管理后台、用户端三个服务进程均不存在。

### 2.4 项目源码核查

| 位置 | 结果 |
|---|---|
| `/workspace` | 仅有 `Dockerfile`、`README.md`（Java 开发环境模板）、`install.sh`；git remote 指向 `werabc/java-dev-env`（开发环境模板，非树洞项目） |
| `/root` `/home` `/opt` `/srv` | 默认配置，无项目代码 |
| `/data` | 仅有沙箱自身数据（skills / design_libraries / browser_snapshots / mcp 配置） |
| 全盘搜索 `package.json`/`pom.xml`/`build.gradle`/`*.jar`/`*.vue` | 未发现任何树洞相关工程文件 |

结论：**沙箱内不存在树洞项目源码**，无法由我自行启动服务。

## 3. 浏览器测试过程与结果

### 3.1 测试步骤

1. `browser_tabs list` → 沙箱浏览器已就绪（初始空白页）。
2. `browser_navigate http://localhost:80/` → 等待 3s 后快照。
3. 重复一次：新标签导航 + 等待 4s + 快照 + 截图。

### 3.2 浏览器实际看到的内容

- **页面 URL**：`http://localhost/`
- **页面标题**：`Service Unavailable`
- **页面内容（DOM 快照原文）**：

```yaml
- role: document
  name: Service Unavailable
  ref: root
  children:
    - role: StaticText
      name: Service not running
      ref: e3
    - role: StaticText
      name: The service has stopped or hasn't been started yet
      ref: e4
    - role: button
      name: Retry
      ref: e5
```

### 3.3 截图证据

浏览器截图已保存：

- `/data/tool/browser_snapshots/port80_state.png`（首次导航）
- `/data/tool/browser_snapshots/port80_retry.png`（重试导航）

两张截图均显示同一占位页：标题 "Service Unavailable"，正文 "Service not running / The service has stopped or hasn't been started yet"，以及一个 "Retry" 按钮。

### 3.4 功能测试矩阵

由于服务实际未运行，所有功能用例均无法执行，状态统一为 **"阻塞 - 服务不可达"**：

| # | 用例 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| TC-01 | 首页加载 | 渲染树洞列表 | "Service not running" 占位页 | 阻塞 |
| TC-02 | 用户注册 | 进入注册流程 | 同上 | 阻塞 |
| TC-03 | 用户登录 | 登录后跳转首页 | 同上 | 阻塞 |
| TC-04 | 发布树洞 | 可填写并提交 | 同上 | 阻塞 |
| TC-05 | 树洞列表 | 展示帖子流 | 同上 | 阻塞 |
| TC-06 | 树洞详情 | 展示单帖+评论 | 同上 | 阻塞 |
| TC-07 | 评论 | 可评论帖子 | 同上 | 阻塞 |
| TC-08 | 点赞/收藏 | 可交互并计数 | 同上 | 阻塞 |
| TC-09 | 个人中心 | 展示用户资料 | 同上 | 阻塞 |
| TC-10 | 搜索 | 可按关键字搜索 | 同上 | 阻塞 |
| TC-11 | 退出登录 | 清除会话 | 同上 | 阻塞 |

## 4. 结论与根因

1. **服务未运行**：用户声明的三个服务在测试沙箱内均未启动。端口 80 被沙箱自身的 `agent-tool-host` 占用并返回统一的"Service not running"占位页；8081/8082 无任何监听。
2. **无源码可启动**：沙箱内不存在树洞项目源码，无法由测试方自行拉起服务。
3. **环境隔离**：测试用浏览器运行在沙箱内，无法访问用户本地机器上的 `localhost` 服务。即便用户本地确实在运行这些服务，沙箱浏览器也无法触达。

因此，本次测试**无法对树洞用户端的任何实际功能进行验证**，所有用例均处于阻塞状态。

## 5. 后续行动建议（任选其一即可解除阻塞）

1. **在沙箱内启动项目（推荐）**：提供树洞项目源码 / git 仓库地址 / 启动脚本（如 `docker-compose.yml`、`mvn spring-boot:run`、`npm run dev` 等），我将在沙箱内依次启动后端(8081)、管理后台(8082)、用户端(80)，然后用浏览器逐项重测并补全本报告。
2. **提供沙箱可访问的远程地址**：若服务部署在公网或公司内网，请提供沙箱可访问的 IP/域名（不能是 `localhost`），我将改用该地址进行浏览器测试。
3. **核对服务状态**：若您认为服务确实在运行，可能是部署在另一台机器或另一沙箱，请告知具体访问方式（含端口转发或跳板信息）。

> 说明：在服务可达之前，本报告中的"阻塞"状态不会改变；后端 API 文档（见同目录 `树洞后端_API文档.md`）也仅能给出占位结构，待后端可达后再补全真实接口。
